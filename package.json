{
  "name": "block-shooter-workspace",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -k --names \"CLIENT,SERVER\" -c \"blue,green\" \"npm run dev -w appClient\" \"npm run dev -w appServer\"",
    "build": "npm run build --workspaces --if-present",
    "start": "concurrently -k --names \"CLIENT,SERVER\" -c \"blue,green\" \"npm run preview -w appClient\" \"npm run start -w appServer\""
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "concurrently": "^9.2.4",
    "nodemon": "^3.1.14",
    "tsx": "^4.23.1",
    "typescript": "^6.0.3"
  }
}
